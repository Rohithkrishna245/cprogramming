#include<stdio.h>
#include<string.h>
int main()
{
char str[50];
printf("enter a string");
scanf("%s",str);
 printf("Reverse the String:");
 for(int i=(strlen(str)-1);i>=0;i--)
 { printf("%c",str[i]);
 } return 0;
}
